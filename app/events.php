<?php

// ------------------------------------------------------------
// Event Listeners
// ------------------------------------------------------------

/*use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Facades\Auth;

Model::creating(function($model)
{
    dd('assd');
    $data['user'] = Auth::user();
    $data['model'] = $model->getClassName(false);

    Log::info('Egy felhasználó Entitást próbál meg felvinni az adatbázisba', $data);
    dd('ok');
});

Model::created(function($model)
{
    $data['user'] = Auth::user();
    $data['model'] = $model->getClassName(false);

    Log::info('Egy felhasználó Entitást próbál meg felvinni az adatbázisba', $data);
    dd('asdasdasds');
});*/